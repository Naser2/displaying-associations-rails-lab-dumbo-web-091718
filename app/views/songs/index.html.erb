<h1> Songs </h1>
<ul>
  <% @songs.each do |song| %>
  <li><%= song.title %></li>
   <a href="<%= song_path(song) %>">
     <!-- <%= link_to song_path(song) %> -->
     <%= song.artist_name %>
     -
     <%= song.title %>
  <% end %>
</ul>
